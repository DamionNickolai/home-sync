CHANGE BRANCH:
git checkout dev

SWISS ARMY KNIFE TO MAKE CHANGES:
git switch dev

CLOSE APP:
Ctrl + c

QUICK SAVE:
Ctrl + s

CREATE VIRTUAL ENV:
python -m venv venv

CREATE LOCAL .ENV FROM TEMPLATE:
Copy-Item maintenance/.env.example .env

CREATE STREAMLIT SECRETS FROM TEMPLATE:
Copy-Item .streamlit/secrets.toml.example .streamlit/secrets.toml

SECRETS HYGIENE (never commit real values):
- .env and .streamlit/secrets.toml are gitignored
- Use *.example files as templates only
- Rotate DB password if it appeared in terminal history or chat

BACKFILL AUTH IDS:
Run locally after setting SUPABASE_URL and SUPABASE_SERVICE_KEY:
python maintenance/backfill_auth_user_ids.py
python maintenance/backfill_auth_user_ids.py --apply

VALIDATE ALL CLOUD:
git fetch --all
git branch -vv
STORE AND MERGE LOCAL UPDATES (Do this if you have made changes locally but origin is ahead):
git stash
git fetch origin dev
git status
git stash pop
JOINT WORKSPACE (HOME + GET FIT):
code home-sync-get-fit.code-workspace

SYNC BOTH APPS FAST:
git -C . fetch origin dev
git -C . status
git -C ../get-fit-together fetch origin dev
git -C ../get-fit-together status

PUSH A SMALL FIX TO DEV STAGING:
git add auth.py
git commit --amend --no-edit
git push --force origin dev

RUN DATABASE MIGRATIONS:
python maintenance/migrate_projects.py

AUDIT PROD VS DEV SCHEMA (read-only):
Add SUPABASE_DB_PASSWORD to .env (see maintenance/.env.example), then:
python maintenance/audit_schema_parity.py
python maintenance/audit_schema_parity.py --json
PowerShell one-liner (prefer .env instead):
$env:SUPABASE_DB_PASSWORD = "your-db-password"

APPLY SCHEMA PARITY TO PROD (dry-run first, then --apply):
python maintenance/apply_schema_parity.py
python maintenance/apply_schema_parity.py --apply

RUN INCOME STREAM BACKFILL (after migration 023):
python maintenance/backfill_income_streams.py              # prod, dry-run
python maintenance/backfill_income_streams.py --local      # dev tables
python maintenance/backfill_income_streams.py --apply      # prod, write
python maintenance/backfill_income_streams.py --local --apply

INCOME STREAMS (migrations 023 + 024 + backfill):
  apply_schema_parity.py --apply runs 022, 023, 024, 025, and 026 automatically.
  Or paste 023 then 024 in Supabase SQL Editor.
  Test schedule logic locally (no DB):
  python -m unittest tests.test_income_pay_occurrences -v
  Then backfill legacy recurring rows:
  python maintenance/backfill_income_streams.py --apply

EXPENSE STREAMS (migrations 025 + 026 + 027 + backfill):
  apply_schema_parity.py --apply includes 025, 026, and 027 (027 fixes dev category_id UUID).

PROJECT FUNDS ROLLOVER (migrations 028 + 029):
  apply_schema_parity.py --apply includes 028 and 029.
  029 fixes projects_funds_opening TEXT if 028 created it as NUMERIC (encrypted values).
  Or run migrations/029_projects_funds_opening_text.sql in Supabase SQL Editor (dev).
  Test bill schedule logic locally (no DB):
  python -m unittest tests.test_expense_bill_occurrences -v
  Backfill legacy recurring expense rows:
  python maintenance/backfill_expense_streams.py              # prod, dry-run
  python maintenance/backfill_expense_streams.py --local      # dev tables
  python maintenance/backfill_expense_streams.py --apply
  python maintenance/backfill_expense_streams.py --local --apply

RESET DEV HOUSEHOLD DATA (E2E testing — *_dev tables only):
  python maintenance/reset_dev_household_data.py --list-households
  python maintenance/reset_dev_household_data.py --household-id test_home
  python maintenance/reset_dev_household_data.py --household-id test_home --apply

BACKFILL ENCRYPTED SESSION TOKENS:
.\venv\Scripts\python.exe maintenance/migrate_user_sessions_refresh_tokens.py
.\venv\Scripts\python.exe maintenance/migrate_user_sessions_refresh_tokens.py --apply

RESET MEMBER TRANSFERS IN DEV:
python maintenance/cleanup_disbursement_phantoms.py --household-id test_home --month-year 2026-06 --apply

INSTALL PACKAGES:
pip install -r requirements.txt

VALIDATE LOCAL MATCHES CLOUD:
git fetch origin dev
git status

AUTO PUSH FROM LOCAL TO CLOUD:
python push_to_dev.py
python push_to_prod.py

ENTER VIRTUAL ENV:
.\venv\Scripts\activate

RUN APP:
python -m streamlit run home_sync.py